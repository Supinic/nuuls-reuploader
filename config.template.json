{
  "twitch": {
    "channels": []
  },
  "reuploader": "imgur OR catbox.moe",
  "imgur": {
    "clientID": "imgur clientid"
  },
  "dataStorage": "mariadb",
  "mariadb": {
    "auth": {
      "host": "mariadb host (incompatible with socket)",
      "socket": "mariadb socket (incompatible with host)",
      "port": "mariadb port",
      "username": "mariadb user",
      "password": "mariadb password"
    },
    "schema": "nuuls-to-imgur",
    "table": "Archive",
    "columns": {
      "added": "Added",
      "nuuls": "Nuuls_ID",
      "reupload": "Reupload_Link"
    }
  }
}
